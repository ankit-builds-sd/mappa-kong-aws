import { irr } from 'node-irr';
import * as utilities from '../../../utilities/utility';
export function circumference__q_circumference(circumference__st_radius) {
  return 2 * Math.PI * circumference__st_radius;
}
export function circumference__q_sum(circumference__q_circumference) {
  return 1 + 2 + 3;
}
